<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.devmjs.basiccalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="AllowBackup">
        <activity
            android:name="com.devmjs.basiccalculator.usecases.calculator.CalculatorActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|uiMode|colorMode"
            android:windowSoftInputMode="stateAlwaysHidden|adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".usecases.history.HistoryActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|uiMode|colorMode"
            android:windowSoftInputMode="stateAlwaysHidden|adjustNothing" />
    </application>
</manifest>